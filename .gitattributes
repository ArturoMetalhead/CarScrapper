# Shell scripts must keep LF endings even on Windows checkouts — CRLF breaks the
# container shebang ("exec /app/entrypoint.sh: no such file or directory").
*.sh text eol=lf
entrypoint.sh text eol=lf
worker-entrypoint.sh text eol=lf
